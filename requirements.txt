# === AUTOMAÇÃO WEB MODERNA ===
# Playwright como principal (substitui Selenium completamente)
playwright==1.53.0
# Undetected-chromedriver apenas para casos específicos de fallback
undetected-chromedriver==3.5.5

# === INTERFACE MODERNA ===
# DearPyGUI para interface principal nativa
dearpygui==1.11.1
# Rich para logs coloridos e formatação estruturada
rich==14.0.0
# Loguru para logging avançado e estruturado
loguru==0.7.2

# === VISÃO COMPUTACIONAL & CAPTCHA ===
# OpenCV para detecção de elementos visuais e processamento de imagem
opencv-python==4.11.0.86
# Tesseract para OCR de captchas e reconhecimento de texto
pytesseract==0.3.13
# Pillow para manipulação básica de imagens
Pillow==11.0.0
# Scikit-image para processamento avançado de imagem
scikit-image==0.24.0

# === CORE LIBRARIES ===
# NumPy 2.x para operações matemáticas e arrays rápidos
numpy==2.2.6
# Scikit-learn para decisões inteligentes e machine learning
scikit-learn==1.5.2
# Requests para HTTP requests simples
requests==2.32.3
# Python-dotenv para gerenciamento de variáveis de ambiente
python-dotenv==1.0.1

# === DADOS E CONFIGURAÇÃO ===
# Pydantic para validação de dados e configurações
pydantic==2.9.2
# PyYAML para arquivos de configuração avançados
PyYAML==6.0.2

# === CLI & CONFIGURAÇÃO ===
# Typer para interface de linha de comando moderna com Rich integration
typer[rich]==0.15.1
# Click como base para CLI
click==8.1.7

# === DESENVOLVIMENTO & QUALIDADE ===
# Ruff para formatação e linting ultra-rápido (substitui Black + Flake8)
ruff==0.7.4
# MyPy para verificação rigorosa de tipos
mypy==1.13.0

# === TESTES ===
# Pytest para testes automatizados modernos
pytest==8.3.3
# Pytest-cov para coverage de testes
pytest-cov==6.0.0

# === TIPAGEM (Type Hints) ===
# Types para requests HTTP
types-requests==2.32.0.20241016
# Types para PyYAML
types-PyYAML==6.0.12.20240917
